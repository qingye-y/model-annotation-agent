# 商品审核大模型质检标注工作台 - 项目总结

> 更新时间：2026-05-11
> 项目代号：OpenClaw v3 / 小龙虾

---

## 一、项目定位

这是一个面向**商品审核大模型质检**的全流程标注工作台，覆盖从数据拉取、机审任务管理、人工标注、质检修正到 Badcase 管理的完整闭环。

采用 **纯静态 HTML 前端 + Flask 后端** 的架构，前端通过 iframe 方式加载各子页面。

---

## 二、前端部分（8 个页面 + 主框架）

### 主框架页

| 文件 | 功能 |
|------|------|
| `index.html` | 主框架，左侧导航栏（9个菜单）+ 顶部用户信息栏 + 右侧 iframe 内容区。支持侧边栏收起/展开，状态持久化到 localStorage |

### 子页面（通过 iframe 加载）

| 页面 | 功能描述 |
|------|----------|
| **dashboard.html** | 数据看板 — 展示总数据量、合规/违规分布、准确率、不一致率等核心指标，含 ApexCharts 图表 |
| **model_task.html** | 机审任务中心 — 实例筛选（5个实例）、规则筛选、时间范围、分页数据展示、状态监控 |
| **dispatch_center.html** | 任务调度中心 — Tab 切换（规则调度/实例调度）、规则卡片管理、调度概览统计 |
| **annotation_list.html** | 标注任务 — 核心标注页面，支持批量分配、标注状态筛选、商品详情弹窗、标注结果提交 |
| **qc_center.html** | 质检修正中心 — 质检概览统计、抽检配置、质检任务列表、修正操作 |
| **badcase_center.html** | Badcase 管理中心 — Badcase 筛选（按实例/规则/时间/状态）、统计卡片、数据表格、详情查看 |
| **get_log.html** | 获取日志 — 数据拉取日志查询、批次详情、日志导出 |
| **account_management.html** | 账号管理 — 标注员账号的增删改查、权限分配、密码重置、日配额管理 |
| **settings.html** | 系统配置 — 标注标签管理、提示词规则配置、实例管理、规则与实例绑定关系 |

### 前端基础设施

| 文件 | 作用 |
|------|------|
| `common.css` | 全局样式系统，定义了 CSS 变量（主色、功能色、布局、圆角、阴影等）和基础组件样式 |
| `config.js` | 前端配置中心，管理标注标签、提示词规则、实例列表、规则-实例绑定关系、标注员用户数据，全部持久化到 localStorage |

---

## 三、后端部分（Flask + SQLite）

### 技术栈

- **Flask 2.3.3** + **Flask-SQLAlchemy** + **Flask-Login** + **PyJWT** + **requests**

### 数据库模型（4 张表）

| 表名 | 作用 |
|------|------|
| `raw_data` | 原始商品数据 — 存储从 iData 拉取的所有商品审核字段（供应商ID、AI审核结果、人审结果、拒绝原因、商品图片链接、商品链接、实例编码等 30+ 字段） |
| `fetch_log` | 拉取日志 — 记录每次数据拉取的批次号、环境、实例、抽样比例、拉取数量、合规/违规统计 |
| `user` | 用户账号 — 用户名、密码哈希、角色(admin/annotator)、姓名、日配额、是否激活 |
| `annotation` | 标注记录 — 关联 raw_data 和 user，记录标注结果、错误标签、备注、提交状态 |

### 蓝图（3 个模块）

| 蓝图 | 路由前缀 | 功能 |
|------|----------|------|
| `auth.py` | `/` | 登录页面 `/login`、API 登录 `/api/auth/login`（返回 JWT Token）、登出、当前用户信息、用户管理 CRUD（增删改查）、密码重置 |
| `data_fetch.py` | `/api/data-fetch` | **核心数据拉取逻辑** — 动态构建 SQL、分页查询 iData、随机抽样、数据入库、记录日志、触发模型 B 互检（Mock） |
| `dashboard.py` | `/api` | 统计 API：总览 `/overview`、按实例 `/by-instance`、按用户 `/by-user`、趋势 `/trend`、日志 `/logs`、综合统计 `/stats` |

### 配置（config.py）

- 双环境支持：**云环境**（3 个实例：浙江网超/浙江乐采网超/湖南乐采网超）和 **乐采云环境**（2 个实例：云南乐采云/广西乐采云）
- 实例中文名称映射、规则 ID 映射
- iData Cookie 配置（需手动填入）
- 分页拉取参数（每页 500 条）

---

## 四、核心数据流

```
┌─────────────┐     SQL 查询      ┌──────────────┐
│   iData     │ ◄──────────────── │  Flask 后端   │
│  数据平台    │   (带 Cookie)    │  data_fetch  │
└─────────────┘                   └──────┬───────┘
                                         │
                    ┌────────────────────┼────────────────────┐
                    ▼                    ▼                    ▼
              ┌─────────┐        ┌────────────┐        ┌──────────┐
              │ SQLite  │        │   随机抽样  │        │ 模型B互检 │
              │ raw_data│        │  (可配置%) │        │  (Mock)  │
              └─────────┘        └────────────┘        └──────────┘
                    │
                    ▼
              ┌─────────┐
              │ 标注任务  │ ◄── 标注员在 annotation_list.html 中标注
              │annotation│      （正确/错误 + 错误标签 + 备注）
              └─────────┘
                    │
                    ▼
              ┌─────────┐
              │ 质检修正  │ ◄── 质检员在 qc_center.html 中复核修正
              │  /报表   │
              └─────────┘
```

---

## 五、已实现的关键能力

| 能力 | 状态 |
|------|------|
| 主框架 + 侧边栏导航 + iframe 页面切换 | 完成 |
| 数据看板（统计卡片 + ApexCharts 图表） | 完成 |
| 机审任务中心（实例/规则/时间筛选 + 分页） | 完成 |
| 任务调度中心（规则调度 + 实例调度 Tab） | 完成 |
| 标注任务列表（分配、筛选、详情弹窗） | 完成 |
| 质检修正中心（概览 + 配置 + 任务列表） | 完成 |
| Badcase 管理中心（筛选 + 统计 + 表格） | 完成 |
| 获取日志（拉取记录查询） | 完成 |
| 账号管理（标注员 CRUD + 配额） | 完成 |
| 系统配置（标签/规则/实例/绑定关系） | 完成 |
| Flask 后端 + SQLite 数据库 | 完成 |
| iData 数据自动拉取（SQL 构建 + 分页 + 抽样） | 完成 |
| 用户认证（JWT Token + Flask-Login） | 完成 |
| 看板统计 API（按实例/按用户/趋势/日志） | 完成 |
| 前端配置持久化（localStorage） | 完成 |

---

## 六、待完善 / 注意事项

| 项目 | 说明 |
|------|------|
| iData Cookie | `config.py` 中 `IDATA_COOKIE` 为空字符串，需手动填入真实 Cookie 才能拉取数据 |
| 前端与后端 API 对接 | 目前前端大部分数据仍走 localStorage Mock，尚未全面接入后端 API |
| 模型 B 互检 | 目前是 Mock（sleep 3 秒后打印日志），未接真实模型 |
| 登录页面 | 后端有 `/login` 路由，但前端主框架直接打开，未强制跳转登录 |
| 图表渲染 | 部分图表可能因无数据而显示为空，需接入真实数据后验证 |

---

## 七、项目文件一览

```
模型标注agent/
├── index.html              # 主框架
├── dashboard.html          # 数据看板
├── model_task.html         # 机审任务
├── dispatch_center.html    # 任务调度
├── annotation_list.html    # 标注任务（核心）
├── qc_center.html          # 质检修正
├── badcase_center.html     # Badcase管理
├── get_log.html            # 获取日志
├── account_management.html # 账号管理
├── settings.html           # 系统配置
├── common.css              # 全局样式
├── config.js               # 前端配置中心
├── app.py                  # Flask 入口
├── models.py               # 数据库模型
├── config.py               # 后端配置
├── requirements.txt        # Python 依赖
├── README-backend.md       # 后端文档
├── 项目总结.md              # 本文件
├── blueprints/
│   ├── __init__.py
│   ├── auth.py             # 认证 + 用户管理
│   ├── data_fetch.py       # 数据拉取核心
│   └── dashboard.py        # 统计 API
└── .workbuddy/memory/      # 会话记忆
```

---

## 八、启动方式

```bash
cd /Users/zcy/Desktop/模型标注agent
pip install -r requirements.txt
python3 app.py
```

访问 http://localhost:5000

默认管理员账号：`admin` / `admin123`

---

*一句话总结：前端 9 个页面的完整 UI 原型 + Flask 后端（用户认证、iData 数据拉取、SQLite 存储、统计 API）已全部搭建完毕，具备从数据拉取到标注质检的完整链路能力，目前主要待办是填入 iData Cookie 实现真实数据打通以及前端全面接入后端 API。*
